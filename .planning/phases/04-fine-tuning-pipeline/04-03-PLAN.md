---
phase: 04-fine-tuning-pipeline
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - scripts/serve_finetuned.py
  - scripts/merge_and_deploy.py
  - scripts/benchmark_compare.py
autonomous: false
requirements: [LAKE-09]
user_setup: []

must_haves:
  truths:
    - "mlx_lm.server can serve the fine-tuned model with adapter loaded via --adapter-path"
    - "Fine-tuned model generates code via OpenAI-compatible API on localhost:8801"
    - "Optional Ollama deployment pipeline exists (may fall back to mlx_lm.server)"
    - "Benchmark comparison script can measure both base and fine-tuned models"
  artifacts:
    - path: "scripts/serve_finetuned.py"
      provides: "Launch mlx_lm.server with fine-tuned adapter"
      min_lines: 80
    - path: "scripts/merge_and_deploy.py"
      provides: "Fuse adapter + optional Ollama import"
      min_lines: 80
    - path: "scripts/benchmark_compare.py"
      provides: "Base vs fine-tuned model comparison"
      min_lines: 120
  key_links:
    - from: "scripts/serve_finetuned.py"
      to: "training/adapters/"
      via: "--adapter-path flag to mlx_lm.server"
      pattern: "adapter.path"
    - from: "scripts/merge_and_deploy.py"
      to: "training/adapters/"
      via: "--adapter-path to mlx_lm.fuse"
      pattern: "mlx_lm.fuse"
    - from: "scripts/benchmark_compare.py"
      to: "scripts/serve-mlx.py"
      via: "launches base model server for comparison"
      pattern: "serve"
---

<objective>
Create the adapter serving script, optional Ollama merge/deploy pipeline, and base-vs-fine-tuned benchmark comparison tool.

Purpose: LAKE-09 requires adapter output compatible with Ollama GGUF merging. Per RESEARCH finding, GGUF export does NOT support Qwen2 (hardcoded llama arch), so the PRIMARY serving path is mlx_lm.server with native --adapter-path. An optional Ollama deployment attempt via experimental safetensors import is provided as fallback.

Output: Three scripts enabling full fine-tuned model serving and evaluation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-fine-tuning-pipeline/04-RESEARCH.md

<interfaces>
<!-- Imports from Plan 01 and Plan 02 outputs -->

From scripts/serve-mlx.py (Phase 3, existing pattern):
```python
DEFAULT_HOST = "0.0.0.0"
DEFAULT_PORT = 8800
MODEL_DIR = os.path.join(os.path.dirname(os.path.dirname(os.path.abspath(__file__))),
                          "models", "qwen2.5-coder-7b-instruct-4bit-mlx")
# Uses subprocess.Popen for mlx_lm.server, wait_for_server polling, SIGTERM handling
```

From RESEARCH -- Adapter serving (Pattern 3):
```bash
mlx_lm.server \
    --model models/qwen2.5-coder-7b-instruct-4bit-mlx \
    --adapter-path training/adapters
```

From RESEARCH -- Ollama import (experimental):
```bash
mlx_lm.fuse --model ... --adapter-path ... --save-path training/fused_model --dequantize
ollama create qwen2.5-coder-ft --experimental -f /tmp/Modelfile.qwen-ft
```

From RESEARCH -- GGUF limitation (Pitfall 1):
`mlx_lm.fuse --export-gguf` hardcodes "llama" architecture metadata. Does NOT work for Qwen2.
Use mlx_lm.server native adapter serving as PRIMARY path.

From RESEARCH -- Disk constraint (Pitfall 2):
27GB free, fused bf16 model ~14GB. Warn before fusion. Recommend mlx_lm.server path.

From existing scripts/serve-mlx.py pattern:
- Health polling via `/v1/models` endpoint
- subprocess.Popen with stdout/stderr streaming
- SIGINT/SIGTERM signal handlers for graceful shutdown
- argparse CLI interface
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create fine-tuned model serving script</name>
  <files>
    scripts/serve_finetuned.py
  </files>
  <read_first>
    scripts/serve-mlx.py (existing server launcher pattern)
    .planning/phases/04-fine-tuning-pipeline/04-RESEARCH.md (Pattern 3: Adapter Serving)
  </read_first>
  <action>
    Create a fine-tuned model server launcher based on the existing `scripts/serve-mlx.py` pattern.

    `scripts/serve_finetuned.py` MUST:
    - Accept `--host` (default: `0.0.0.0`), `--port` (default: `8801` -- different port from base model on 8800), `--model` (default: `models/qwen2.5-coder-7b-instruct-4bit-mlx`), `--adapter-path` (default: `training/adapters`)
    - Verify model directory exists
    - Verify adapter directory exists (contains `adapter_config.json` or `adapters.safetensors`)
    - Launch `mlx_lm.server --model <model> --adapter-path <adapter> --host <host> --port <port>`
    - Use same health polling pattern as serve-mlx.py (poll `/v1/models` until 200 or timeout)
    - Stream server output
    - Handle SIGINT/SIGTERM for graceful shutdown
    - On ready, print: `Fine-tuned model ready on http://localhost:<port>` plus note that model ID in API will be the full model path (per Phase 3 finding)
    - Exit 0 on clean shutdown, exit 1 on error
    - Use `#!/usr/bin/env python3` shebang and `argparse`
  </action>
  <verify>
    <automated>cd /Users/bretbouchard/apps/me_code_pretty_one_day && python3 -c "import ast; ast.parse(open('scripts/serve_finetuned.py').read()); print('Parseable')" && python3 scripts/serve_finetuned.py --help</automated>
  </verify>
  <done>
    - `scripts/serve_finetuned.py` exists, parses without errors
    - Accepts --adapter-path flag defaulting to training/adapters
    - Default port 8801 (does not conflict with base model on 8800)
    - Follows same pattern as serve-mlx.py with health polling and graceful shutdown
  </done>
</task>

<task type="auto">
  <name>Task 2: Create merge/deploy script and benchmark comparison</name>
  <files>
    scripts/merge_and_deploy.py
    scripts/benchmark_compare.py
  </files>
  <read_first>
    .planning/phases/04-fine-tuning-pipeline/04-RESEARCH.md (Pitfall 1: GGUF, Pitfall 2: Disk Space, Ollama Import section)
    scripts/benchmark-latency.py (existing TTFT benchmark pattern)
    scripts/benchmark-throughput.py (existing throughput benchmark pattern)
  </read_first>
  <action>
    Create the adapter fusion/deploy script and the base-vs-fine-tuned comparison benchmark.

    1. **`scripts/merge_and_deploy.py`** -- Adapter fusion and optional Ollama deployment:
       - Accept `--model` (default: base model path), `--adapter-path` (default: `training/adapters`), `--output` (default: `training/fused_model`), `--deploy-ollama` (flag), `--skip-disk-check` (flag), `--dequantize` (flag, default true for Ollama path)
       - Steps:
         a. Check disk space: require >= 15GB free for dequantize (fused bf16 ~14GB). Warn if < 20GB. Error if < 15GB and --dequantize. If not --dequantize, require >= 2GB.
         b. Verify adapter exists (adapter_config.json or adapters.safetensors)
         c. Run `mlx_lm.fuse --model <model> --adapter-path <adapter> --save-path <output> [--dequantize]`
         d. Print fusion result (file count, total size)
         e. If `--deploy-ollama`:
            - Create Modelfile: `FROM <absolute_path_to_fused_model>` + `PARAMETER temperature 0.7` + `PARAMETER num_ctx 4096`
            - Run `ollama create qwen2.5-coder-ft --experimental -f <Modelfile>`
            - If succeeds: print success with `ollama run qwen2.5-coder-ft "test prompt"` suggestion
            - If fails: print warning that Ollama path failed, recommend `python3 scripts/serve_finetuned.py` instead
       - **DO NOT** use `--export-gguf` flag. Per RESEARCH Pitfall 1, it hardcodes llama architecture and does NOT work for Qwen2.
       - Exit 0 on success, exit 1 on fusion failure, exit 0 on Ollama failure (just warn)

    2. **`scripts/benchmark_compare.py`** -- Base vs fine-tuned model comparison:
       - Accept `--base-url` (default: `http://localhost:8800`), `--ft-url` (default: `http://localhost:8801`), `--prompts-file` (default: `data/valid.jsonl`), `--num-prompts` (default: `20`), `--max-tokens` (default: `256`)
       - Read user prompts from valid.jsonl (extract the user message content)
       - For each prompt, send to BOTH base model and fine-tuned model:
         - Use OpenAI-compatible `/v1/chat/completions` endpoint
         - Use non-streaming for simplicity (measure total generation time)
       - Collect per-prompt results: response length, generation time, code quality heuristic
       - Code quality heuristic: check if response contains valid Python/Swift syntax (try `ast.parse` for Python, simple bracket matching for Swift), has docstrings, has type hints
       - Print comparison table:
         ```
         === Base vs Fine-Tuned Comparison ===
         Model          | Avg Time | Avg Tokens | Syntax OK | Has Docstrings | Has Types
         base           | 2.3s     | 145        | 80%       | 60%           | 40%
         fine-tuned     | 2.1s     | 160        | 95%       | 85%           | 70%
         ```
       - Print summary: improvement percentage for each metric
       - Use `urllib.request` only (no `requests` library) per existing script conventions
       - Handle model ID discovery: query `/v1/models` to get the correct model ID (per Phase 3 finding, mlx_lm.server uses full path as model ID)
       - Use `#!/usr/bin/env python3` shebang and `argparse`
  </action>
  <verify>
    <automated>cd /Users/bretbouchard/apps/me_code_pretty_one_day && python3 -c "import ast; ast.parse(open('scripts/merge_and_deploy.py').read()); print('merge deploy parseable')" && python3 -c "import ast; ast.parse(open('scripts/benchmark_compare.py').read()); print('benchmark compare parseable')" && python3 scripts/merge_and_deploy.py --help && python3 scripts/benchmark_compare.py --help</automated>
  </verify>
  <done>
    - `scripts/merge_and_deploy.py` exists, fuses adapter into base model, optionally deploys to Ollama
    - Merge script checks disk space before dequantization
    - Merge script does NOT use --export-gguf (Qwen2 incompatible per RESEARCH)
    - Merge script warns on Ollama failure, recommends mlx_lm.server path
    - `scripts/benchmark_compare.py` exists, compares base vs fine-tuned on validation prompts
    - Benchmark outputs comparison table with improvement percentages
    - Both scripts use urllib.request only, follow existing script conventions
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Three scripts for fine-tuned model serving, deployment, and comparison:
    - scripts/serve_finetuned.py (mlx_lm.server with adapter)
    - scripts/merge_and_deploy.py (fuse + optional Ollama)
    - scripts/benchmark_compare.py (base vs fine-tuned comparison)
  </what-built>
  <how-to-verify>
    1. Ensure Plan 01 (training data) and Plan 02 (training config) are complete
    2. Run training: `cd /Users/bretbouchard/apps/me_code_pretty_one_day && python3 scripts/train_lora.py`
    3. Wait for training to complete (adapter saved to training/adapters/)
    4. Start fine-tuned server: `python3 scripts/serve_finetuned.py`
    5. Test generation: `curl http://localhost:8801/v1/chat/completions -d '{"model":"(discovered-model-id)","messages":[{"role":"user","content":"Write a Python function to find prime numbers"}],"max_tokens":200}'`
    6. Optionally test Ollama deployment: `python3 scripts/merge_and_deploy.py --deploy-ollama`
    7. Run comparison: `python3 scripts/benchmark_compare.py`
  </how-to-verify>
  <resume-signal>Type "approved" if fine-tuned model serves correctly and benchmark shows improvement. Describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter -> mlx_lm.server | Trusted local adapter loaded by local framework |
| benchmark -> base/ft servers | OpenAI-compatible API calls to local servers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | D (Denial of service) | Disk exhaustion from fusion | mitigate | Disk check >= 15GB before dequantize; warn at < 20GB |
| T-04-07 | S (Spoofing) | Ollama model name collision | accept | Model name `qwen2.5-coder-ft` is unique, local-only |
</threat_model>

<verification>
1. All three scripts parse without syntax errors
2. serve_finetuned.py --help shows --adapter-path and --port args
3. merge_and_deploy.py --help shows --deploy-ollama and --dequantize args
4. benchmark_compare.py --help shows --base-url and --ft-url args
5. merge_and_deploy.py does NOT contain --export-gguf anywhere (grep verification)
</verification>

<success_criteria>
- Fine-tuned model can be served via mlx_lm.server with native adapter loading
- Optional Ollama deployment pipeline available (with fallback to mlx_lm.server)
- Benchmark comparison shows measurable difference between base and fine-tuned models
- All scripts follow project conventions (argparse, urllib, shebang)
</success_criteria>

<output>
After completion, create `.planning/phases/04-fine-tuning-pipeline/04-03-SUMMARY.md`
</output>
