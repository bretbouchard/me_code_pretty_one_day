---
phase: 04-fine-tuning-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate_training_data.py
  - data/seed_examples/python_tasks.json
  - data/seed_examples/swift_tasks.json
  - data/seed_examples/general_tasks.json
  - scripts/conftest.py
  - scripts/test_generate_training_data.py
  - scripts/test_training_data_format.py
  - scripts/test_train_val_split.py
autonomous: true
requirements: [LAKE-07, LAKE-08, LAKE-10]
user_setup: []

must_haves:
  truths:
    - "Training data generator produces 5000+ code examples in chat JSONL format"
    - "Each example follows standard {messages: [{role, content}]} format (NOT raw ChatML tokens)"
    - "Train/val split is 90/10 with reproducible seed 42"
    - "Running the generator twice produces identical train.jsonl and valid.jsonl"
  artifacts:
    - path: "scripts/generate_training_data.py"
      provides: "Synthetic training data generation from seed examples"
      min_lines: 100
    - path: "data/seed_examples/python_tasks.json"
      provides: "Python code generation seed tasks"
      contains: "python"
    - path: "data/seed_examples/swift_tasks.json"
      provides: "Swift code generation seed tasks"
      contains: "swift"
    - path: "data/seed_examples/general_tasks.json"
      provides: "General code generation seed tasks"
    - path: "scripts/conftest.py"
      provides: "Shared pytest fixtures for test scripts"
      contains: "pytest"
    - path: "data/train.jsonl"
      provides: "Training examples (90% of dataset)"
      min_lines: 4500
    - path: "data/valid.jsonl"
      provides: "Validation examples (10% of dataset)"
      min_lines: 500
  key_links:
    - from: "scripts/generate_training_data.py"
      to: "data/train.jsonl"
      via: "writes JSONL output with seed=42 shuffle"
      pattern: "random\\.seed\\(42\\)"
    - from: "scripts/generate_training_data.py"
      to: "data/seed_examples/"
      via: "reads seed task files for prompt templates"
      pattern: "seed_examples"
    - from: "scripts/generate_training_data.py"
      to: "training/lora_config.yaml"
      via: "produces data/ directory expected by mlx_lm.lora config"
      pattern: "data/"
---

<objective>
Create the training data generator that produces 5000+ code generation examples in standard chat JSONL format, plus seed task files, shared test fixtures, and unit tests.

Purpose: LAKE-07 requires 5000+ training examples, LAKE-08 requires Qwen ChatML format (applied automatically by mlx-lm from standard chat JSONL), LAKE-10 requires deterministic 90/10 train/val split.

Output: Functional data generator script, seed task files, populated train.jsonl and valid.jsonl, and passing unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-fine-tuning-pipeline/04-RESEARCH.md

<interfaces>
<!-- No imports from other Phase 4 plans needed (this is Wave 1) -->

From existing scripts (Phase 3 pattern):
- All scripts use `#!/usr/bin/env python3` shebang
- Use stdlib `urllib.request` for HTTP calls (no `requests` dependency)
- Scripts accept CLI args via `argparse`
- Project root: `/Users/bretbouchard/apps/me_code_pretty_one_day/`
- Base model path: `./models/qwen2.5-coder-7b-instruct-4bit-mlx/`
- Relative paths resolved via `os.path.dirname(os.path.abspath(__file__))`

Training data format (from RESEARCH Pattern 2):
```jsonl
{"messages": [{"role": "system", "content": "..."}, {"role": "user", "content": "..."}, {"role": "assistant", "content": "..."}]}
```

mlx-lm applies Qwen ChatML template automatically from this format. Do NOT write raw `<|im_start|>` tokens into JSONL.

Train/val split (from RESEARCH Pattern 4):
```python
import random
random.seed(42)
random.shuffle(examples)
split_idx = int(len(examples) * 0.9)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create seed task files and training data generator</name>
  <files>
    data/seed_examples/python_tasks.json
    data/seed_examples/swift_tasks.json
    data/seed_examples/general_tasks.json
    scripts/generate_training_data.py
  </files>
  <read_first>
    .planning/phases/04-fine-tuning-pipeline/04-RESEARCH.md (Pattern 2: Chat Format, Pattern 4: Train/Val Split, Pitfall 3: Data Quality)
  </read_first>
  <behavior>
    - Test 1: generate_training_data.py exists and is importable without errors
    - Test 2: Running generator produces data/train.jsonl with >= 4500 lines
    - Test 3: Running generator produces data/valid.jsonl with >= 500 lines
    - Test 4: Each line in train.jsonl is valid JSON with "messages" key containing list of dicts with "role" and "content"
    - Test 5: Each message has role in {"system", "user", "assistant"}
    - Test 6: Each example has exactly 3 messages (system, user, assistant)
    - Test 7: No raw ChatML tokens (<|im_start|>) appear in any content field
    - Test 8: Running generator twice produces byte-identical train.jsonl (determinism)
    - Test 9: Total examples = len(train) + len(valid), valid is ~10% of total
    - Test 10: Seed task files exist with at least 3 entries each
  </behavior>
  <action>
    Create the synthetic training data generator. The script MUST:

    1. **Seed task files** (`data/seed_examples/`):
       - `python_tasks.json`: Array of 15+ Python code generation task templates with system prompts, user prompts, and expected patterns. Include tasks like: file I/O, data structures, algorithms, decorators, context managers, async, type hints, testing, CLI tools, API clients.
       - `swift_tasks.json`: Array of 10+ Swift code generation task templates covering: SwiftUI views, Combine publishers, async/await, Codable, protocols/extensions, property wrappers.
       - `general_tasks.json`: Array of 10+ general tasks covering: regex, SQL generation, shell scripting, data transformation, error handling patterns.

       Each seed entry format:
       ```json
       {"system": "You are an expert Python developer...", "user_prompt": "Write a function that...", "difficulty": "easy|medium|hard", "tags": ["algorithms", "strings"]}
       ```

    2. **Generator script** (`scripts/generate_training_data.py`):
       - CLI args: `--output-dir` (default: `data/`), `--seed` (default: `42`), `--target-count` (default: `5500`), `--max-seq-tokens` (default: `1800`)
       - Read all seed task files from `data/seed_examples/`
       - For each seed task, generate synthetic variations using the base model (mlx_lm.generate) OR template-based expansion if model is unavailable
       - **IMPORTANT per RESEARCH Pitfall 3:** Include quality safeguards:
         - Filter out examples where assistant response is empty or <50 chars
         - Filter out examples exceeding `--max-seq-tokens` (estimate: ~4 chars/token)
         - Ensure diversity: no duplicate user prompts
       - Write output in chat JSONL format: `{"messages": [{"role": "system", "content": "..."}, {"role": "user", "content": "..."}, {"role": "assistant", "content": "..."}]}`
       - Each line is a single valid JSON object (no newlines within object)
       - Shuffle with `random.seed(42)`, then split 90/10 into `train.jsonl` and `valid.jsonl`
       - Print summary: total examples, train count, valid count, avg tokens estimated
       - If base model is available at `models/qwen2.5-coder-7b-instruct-4bit-mlx/`, use `mlx_lm.generate` for high-quality assistant responses. If unavailable, use template-based expansion from seed examples (the generator should work in both modes -- check for model dir existence).
       - Exit 0 on success, exit 1 on failure with error message to stderr

    3. **DO NOT** write raw `<|im_start|>` or `<|im_end|>` tokens. mlx-lm applies the Qwen ChatML template automatically from the standard `{messages}` format.

    4. Create `data/` directory if it does not exist. Create `data/seed_examples/` directory if it does not exist.
  </action>
  <verify>
    <automated>cd /Users/bretbouchard/apps/me_code_pretty_one_day && python3 scripts/generate_training_data.py --output-dir data/ --seed 42 --target-count 5500</automated>
  </verify>
  <done>
    - `data/train.jsonl` exists with >= 4500 lines of valid chat JSONL
    - `data/valid.jsonl` exists with >= 500 lines of valid chat JSONL
    - All examples follow `{"messages": [{"role": "system/user/assistant", "content": "..."}]}` format
    - No raw ChatML tokens in data
    - Running generator again produces identical output files (seed=42 determinism)
    - 90/10 split ratio respected
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create shared pytest fixtures and unit tests</name>
  <files>
    scripts/conftest.py
    scripts/test_generate_training_data.py
    scripts/test_training_data_format.py
    scripts/test_train_val_split.py
  </files>
  <read_first>
    .planning/phases/04-fine-tuning-pipeline/04-RESEARCH.md (Validation Architecture section)
    scripts/generate_training_data.py (created in Task 1)
  </read_first>
  <behavior>
    - Test 1 (test_generate_training_data.py): data directory contains train.jsonl and valid.jsonl after generation
    - Test 2 (test_generate_training_data.py): train.jsonl has >= 4500 lines, valid.jsonl has >= 500 lines
    - Test 3 (test_training_data_format.py): every line in train.jsonl and valid.jsonl is valid JSON with "messages" key
    - Test 4 (test_training_data_format.py): every message dict has "role" in {"system", "user", "assistant"} and non-empty "content" string
    - Test 5 (test_training_data_format.py): every example has last message role == "assistant"
    - Test 6 (test_training_data_format.py): no raw ChatML tokens (<|im_start|>, <|im_end|>) in any content
    - Test 7 (test_train_val_split.py): no example in train.jsonl appears in valid.jsonl (no overlap)
    - Test 8 (test_train_val_split.py): len(train) / (len(train) + len(valid)) is in range [0.89, 0.91]
    - Test 9 (test_train_val_split.py): running split twice with same seed produces same files
  </behavior>
  <action>
    Create test infrastructure for the training data pipeline.

    1. **`scripts/conftest.py`**:
       - Fixture `PROJECT_ROOT`: resolves to project root via `os.path.dirname(os.path.dirname(os.path.abspath(__file__)))`
       - Fixture `DATA_DIR`: `os.path.join(PROJECT_ROOT, "data")`
       - Fixture `TRAIN_FILE`: `os.path.join(DATA_DIR, "train.jsonl")`
       - Fixture `VALID_FILE`: `os.path.join(DATA_DIR, "valid.jsonl")`
       - Fixture `MODEL_DIR`: `os.path.join(PROJECT_ROOT, "models", "qwen2.5-coder-7b-instruct-4bit-mlx")`
       - Fixture `seed_examples_dir`: path to `data/seed_examples/`
       - Fixture `sample_examples` (parametrized): reads N random lines from train.jsonl, parses JSON, returns list of dicts. Skip if train.jsonl does not exist.

    2. **`scripts/test_generate_training_data.py`**:
       - `test_train_file_exists`: train.jsonl exists in data dir
       - `test_valid_file_exists`: valid.jsonl exists in data dir
       - `test_train_count`: train.jsonl has >= 4500 lines
       - `test_valid_count`: valid.jsonl has >= 500 lines
       - `test_seed_files_exist`: all 3 seed JSON files exist
       - `test_seed_files_valid_json`: each seed file is valid JSON array
       - `test_seed_file_min_entries`: each seed file has >= 3 entries

    3. **`scripts/test_training_data_format.py`**:
       - `test_each_line_valid_json`: parametrize over first 100 lines of train + valid
       - `test_messages_key_exists`: each parsed line has "messages" key
       - `test_messages_is_list`: messages value is a list
       - `test_message_roles_valid`: each message has role in {"system", "user", "assistant"}
       - `test_message_content_nonempty`: each message has non-empty string content
       - `test_last_message_is_assistant`: last message in each example has role "assistant"
       - `test_no_raw_chatml_tokens`: no `<|im_start|>` or `<|im_end|>` in any content field (check first 100 examples)
       - `test_system_message_present`: first message has role "system"

    4. **`scripts/test_train_val_split.py`**:
       - `test_no_overlap`: no user prompt in train also appears in valid (compare user message content, sample 200 from each)
       - `test_split_ratio`: train is ~90% of total (tolerance 0.89-0.91)
       - `test_determinism_shuffle`: call the split logic twice with seed=42 on a mock dataset, verify identical order

    All tests should be fast (<10 seconds total). Tests that need actual data files should use `pytest.mark.skipif` if files don't exist yet.
  </action>
  <verify>
    <automated>cd /Users/bretbouchard/apps/me_code_pretty_one_day && python3 -m pytest scripts/test_generate_training_data.py scripts/test_training_data_format.py scripts/test_train_val_split.py -v --tb=short</automated>
  </verify>
  <done>
    - All test files pass with `pytest -v`
    - conftest.py provides shared fixtures used by all test files
    - Tests validate data format, count, split ratio, determinism, and absence of raw ChatML tokens
    - Total test suite runs in <10 seconds
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed_examples -> generator | Seed task templates are trusted (hand-crafted by developer) |
| generator -> model API | Generator calls mlx_lm.generate if model available -- trusted local model |
| JSONL output -> training pipeline | Output files consumed by mlx_lm.lora -- validate format before training |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T (Tampering) | Training data content | mitigate | Validate each example has non-empty assistant response >= 50 chars; filter duplicates |
| T-04-02 | T (Tampering) | Catastrophic forgetting from bad data | mitigate | Use diverse tasks across languages; monitor validation loss during training (Plan 02) |
| T-04-03 | I (Information disclosure) | Seed examples may contain patterns from proprietary code | accept | Training data stays local, never uploaded. No external network calls in generator. |
</threat_model>

<verification>
1. Run `python3 scripts/generate_training_data.py --output-dir data/ --seed 42 --target-count 5500` and verify exit code 0
2. Verify `data/train.jsonl` has >= 4500 lines, `data/valid.jsonl` has >= 500 lines
3. Spot-check 10 random lines from train.jsonl: each is valid JSON with messages array
4. Run `python3 -m pytest scripts/test_generate_training_data.py scripts/test_training_data_format.py scripts/test_train_val_split.py -v` and verify all pass
5. Run generator again with same seed, diff output files: must be identical
</verification>

<success_criteria>
- 5000+ total code generation examples in standard chat JSONL format
- Train/val split at 90/10 with deterministic seed 42
- All unit tests pass
- No raw ChatML tokens in data
- Generator runs to completion without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-fine-tuning-pipeline/04-01-SUMMARY.md`
</output>
