---
phase: 04-fine-tuning-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - training/lora_config.yaml
  - scripts/train_lora.py
autonomous: true
requirements: [LAKE-06]
user_setup: []

must_haves:
  truths:
    - "mlx_lm.lora trains successfully using the config file with QLoRA 4-bit base model"
    - "Adapter files are saved to training/adapters/ after training completes"
    - "Validation loss decreases over training iterations"
    - "Training uses --mask-prompt so loss is only computed on assistant responses"
    - "Disk space is checked before training starts"
  artifacts:
    - path: "training/lora_config.yaml"
      provides: "MLX-LoRA training configuration"
      contains: "lora_parameters"
    - path: "scripts/train_lora.py"
      provides: "Training launch wrapper with disk check and logging"
      min_lines: 60
  key_links:
    - from: "training/lora_config.yaml"
      to: "models/qwen2.5-coder-7b-instruct-4bit-mlx/"
      via: "model path in config"
      pattern: "model:.*qwen2\\.5"
    - from: "training/lora_config.yaml"
      to: "data/train.jsonl"
      via: "data directory in config"
      pattern: "data:"
    - from: "scripts/train_lora.py"
      to: "training/lora_config.yaml"
      via: "passes config to mlx_lm.lora"
      pattern: "lora_config\\.yaml"
---

<objective>
Create the LoRA training configuration and a wrapper script that launches mlx_lm.lora with disk space checks, logging, and proper QLoRA parameters.

Purpose: LAKE-06 requires MLX-LoRA fine-tuning with rank 16-32 and QLoRA 4-bit. This plan creates the config and launcher that the executor will use to train the adapter.

Output: Training config YAML, wrapper script, and after execution the trained adapter at training/adapters/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-fine-tuning-pipeline/04-RESEARCH.md

<interfaces>
<!-- No imports from other Phase 4 plans (Wave 1 parallel) -->

From RESEARCH Pattern 1 (QLoRA Training Config):
```yaml
model: "models/qwen2.5-coder-7b-instruct-4bit-mlx"
train: true
fine_tune_type: lora
data: "data"
seed: 42
num_layers: 16
batch_size: 4
iters: 1000
learning_rate: 5e-6
grad_accumulation_steps: 2
val_batches: 25
steps_per_report: 10
steps_per_eval: 100
save_every: 200
adapter_path: "training/adapters"
lora_parameters:
  keys: ["self_attn.q_proj", "self_attn.k_proj", "self_attn.v_proj", "self_attn.o_proj"]
  rank: 16
  scale: 32.0
  dropout: 0.0
lr_schedule:
  name: cosine_decay
  warmup: 100
  warmup_init: 1e-7
  arguments: [5e-6, 1000, 1e-7]
grad_checkpoint: true
max_seq_length: 2048
```

From RESEARCH -- CLI command equivalent:
```bash
mlx_lm.lora --config training/lora_config.yaml
```

IMPORTANT: `--mask-prompt` is a CLI-only flag, not a YAML config option. The wrapper script must pass it on the CLI.

From existing scripts (Phase 3 pattern):
- Scripts use `#!/usr/bin/env python3` shebang
- CLI args via `argparse`
- Relative paths via `os.path.dirname(os.path.abspath(__file__))`
- Exit codes: 0 success, 1 failure
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LoRA training config and wrapper script</name>
  <files>
    training/lora_config.yaml
    scripts/train_lora.py
  </files>
  <read_first>
    .planning/phases/04-fine-tuning-pipeline/04-RESEARCH.md (Pattern 1: QLoRA Training, Pitfall 2: Disk Space, Anti-Patterns section)
  </read_first>
  <action>
    Create the training configuration and wrapper script for MLX-LoRA fine-tuning.

    1. **`training/lora_config.yaml`** -- Use the exact config from RESEARCH Pattern 1 above. Key parameters:
       - `model`: `"models/qwen2.5-coder-7b-instruct-4bit-mlx"` (relative path from project root)
       - `train`: `true`
       - `fine_tune_type`: `lora`
       - `data`: `"data"` (directory containing train.jsonl and valid.jsonl)
       - `seed`: `42` (LAKE-10 reproducibility)
       - `num_layers`: `16` (last 16 of 28 layers -- memory efficient, proven in kicad-agent)
       - `batch_size`: `4`
       - `iters`: `1000` (start conservative, can increase if loss still decreasing)
       - `learning_rate`: `5e-6` (LAKE-06 requirement)
       - `grad_accumulation_steps`: `2` (effective batch size = 8)
       - `val_batches`: `25`
       - `steps_per_report`: `10`
       - `steps_per_eval`: `100`
       - `save_every`: `200`
       - `adapter_path`: `"training/adapters"`
       - `lora_parameters.rank`: `16` (start with 16 per kicad-agent proven pattern)
       - `lora_parameters.scale`: `32.0` (2x rank, standard practice)
       - `lora_parameters.dropout`: `0.0` (no dropout for code tasks)
       - `lora_parameters.keys`: all 4 attention projections (q, k, v, o)
       - `lr_schedule`: cosine decay with 100-step warmup
       - `grad_checkpoint`: `true` (critical for 32GB Mac memory)
       - `max_seq_length`: `2048` (memory management, filter longer examples)
       - Comments explaining each parameter and why it was chosen

    2. **`scripts/train_lora.py`** -- Wrapper script that:
       - Accepts `--config` (default: `training/lora_config.yaml`), `--skip-disk-check` (flag)
       - Checks disk space before training: require >= 10GB free (shutil.disk_usage). Print warning if < 15GB, error and exit 1 if < 10GB. Per RESEARCH Pitfall 2.
       - Verifies model directory exists at path from config
       - Verifies `data/train.jsonl` and `data/valid.jsonl` exist
       - Creates `training/adapters/` directory if it does not exist
       - Launches `mlx_lm.lora --config <config> --mask-prompt` via subprocess
       - **CRITICAL:** Pass `--mask-prompt` on the CLI. This flag is CLI-only (not supported in YAML config). Per RESEARCH Anti-Patterns: "Not using mask-prompt" wastes training capacity on prompt reproduction instead of response quality.
       - Streams output to stdout
       - On completion, prints adapter path and training summary
       - Handles SIGINT/SIGTERM gracefully (terminate subprocess)
       - Exit 0 on success, exit 1 on failure

    3. Create `training/` directory if it does not exist.

    4. DO NOT actually run training in this task. This plan creates the config and launcher. Training execution happens when the executor runs the script manually or in a subsequent checkpoint.
  </action>
  <verify>
    <automated>cd /Users/bretbouchard/apps/me_code_pretty_one_day && python3 -c "import yaml; c = yaml.safe_load(open('training/lora_config.yaml')); assert c['model'] == 'models/qwen2.5-coder-7b-instruct-4bit-mlx'; assert c['learning_rate'] == 5e-6; assert c['lora_parameters']['rank'] == 16; assert c['seed'] == 42; print('Config valid')" && python3 -c "import ast; ast.parse(open('scripts/train_lora.py').read()); print('Script parseable')" && python3 scripts/train_lora.py --help</automated>
  </verify>
  <done>
    - `training/lora_config.yaml` exists with correct QLoRA parameters
    - Config specifies model path, data dir, rank 16, lr 5e-6, seed 42, grad_checkpoint, max_seq_length 2048
    - `scripts/train_lora.py` launches mlx_lm.lora with --mask-prompt flag
    - Wrapper checks disk space before training
    - Wrapper verifies model dir and data files exist
    - Both files are syntactically valid
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.yaml -> mlx_lm.lora | Training parameters flow into MLX framework -- trusted local tool |
| training data -> training pipeline | Data consumed by mlx_lm.lora -- validated in Plan 01 tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | D (Denial of service) | Disk space exhaustion from training artifacts | mitigate | Disk check before training (>= 10GB free required). Config uses save_every: 200 to limit checkpoint count. |
| T-04-05 | D (Denial of service) | OOM during training | mitigate | grad_checkpoint: true, batch_size: 4, max_seq_length: 2048, grad_accumulation_steps: 2 |
</threat_model>

<verification>
1. Config YAML parses without errors: `python3 -c "import yaml; yaml.safe_load(open('training/lora_config.yaml'))"`
2. Config has all required fields: model, data, seed, learning_rate, lora_parameters.rank, adapter_path
3. Wrapper script parses without syntax errors
4. `--help` flag works on wrapper script
5. Disk check logic functions (test with mocked disk_usage)
</verification>

<success_criteria>
- training/lora_config.yaml contains valid QLoRA config for mlx_lm.lora
- scripts/train_lora.py launches training with --mask-prompt and disk space check
- All parameters match LAKE-06 requirements (rank 16, QLoRA 4-bit, lr 5e-6)
- Training can be launched with: `python3 scripts/train_lora.py`
</success_criteria>

<output>
After completion, create `.planning/phases/04-fine-tuning-pipeline/04-02-SUMMARY.md`
</output>
