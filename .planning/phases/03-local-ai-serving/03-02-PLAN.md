---
phase: 03-local-ai-serving
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - scripts/benchmark-latency.py
  - scripts/benchmark-throughput.py
autonomous: true
requirements: [LAKE-01, LAKE-03, LAKE-04]
user_setup: []
must_haves:
  truths:
    - "Ollama serves Qwen2.5-Coder-7B on localhost:11434 with OpenAI-compatible API"
    - "First token latency (TTFT) is below 5 seconds on M2 Pro 32GB"
    - "Generation throughput is at or above 30 tok/s for sustained generation"
  artifacts:
    - path: "scripts/benchmark-latency.py"
      provides: "TTFT measurement results"
      contains: "def measure_ttft"
    - path: "scripts/benchmark-throughput.py"
      provides: "Throughput measurement results"
      contains: "def measure_throughput"
  key_links:
    - from: "scripts/benchmark-latency.py"
      to: "Ollama API (localhost:11434)"
      via: "stream=True on OpenAI chat completions"
      pattern: "stream=True"
    - from: "scripts/benchmark-throughput.py"
      to: "Ollama API (localhost:11434)"
      via: "streaming token counting"
      pattern: "chunk.choices.*delta.content"
---

<objective>
Configure Ollama serving for Qwen2.5-Coder-7B and validate that latency and throughput meet targets.

Purpose: Establish the primary serving path per D-03 (Ollama for serving), and prove LAKE-03 (sub-5s TTFT) and LAKE-04 (>=30 tok/s) on the M2 Pro 32GB hardware.
Output: Ollama serving confirmed, benchmark results proving performance targets met, serving setup documentation.
</objective>

<execution_context>
@/Users/bretbouchard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bretbouchard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-local-ai-serving/03-01-SUMMARY.md

# Infrastructure (from Plan 3.1)
- Model `qwen2.5-coder:7b-instruct-q4_K_M` available in Ollama
- Ollama 0.30.6 running, daemon on localhost:11434
- Benchmark scripts created: scripts/benchmark-latency.py, scripts/benchmark-throughput.py

# Performance targets
- LAKE-03: Sub-5s first token latency (TTFT) on M-series Mac (7B model)
- LAKE-04: Generation throughput >= 30 tok/s for 7B model

# Expected performance (M2 Pro 32GB, 7B Q4_K_M)
- Research estimate: 25-35 tok/s generation throughput
- TTFT expected: <3s (well under 5s target)

# Ollama configuration options
- OLLAMA_NUM_PARALLEL: Controls parallel request handling (default: 1)
- OLLAMA_MAX_LOADED_MODELS: Controls how many models stay in VRAM (default: 1)
- Ollama Modelfile: Can set system prompt, temperature, and other generation params
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Ollama serving and run benchmarks</name>
  <files>scripts/benchmark-latency.py, scripts/benchmark-throughput.py</files>
  <action>
    Verify Ollama serving and run benchmarks to validate performance targets:

    1. Confirm Ollama daemon is running and model is loaded:
       ```bash
       ollama ps
       ```
       If not loaded, trigger a load with:
       ```bash
       ollama run qwen2.5-coder:7b-instruct-q4_K_M /dev/null
       ```

    2. Verify the OpenAI-compatible API responds:
       ```bash
       curl -s http://localhost:11434/v1/models | python3 -m json.tool
       ```
       Confirm `qwen2.5-coder:7b-instruct-q4_K_M` appears in the models list.

    3. Run the latency benchmark (5 iterations, measures TTFT):
       ```bash
       cd /Users/bretbouchard/apps/me_code_pretty_one_day
       python3 scripts/benchmark-latency.py
       ```
       Record the mean TTFT. Must be <5.0 seconds (LAKE-03).
       If the benchmark scripts from Plan 3.1 have issues, fix them — they must produce valid measurements.

    4. Run the throughput benchmark (3 iterations, measures tok/s):
       ```bash
       cd /Users/bretbouchard/apps/me_code_pretty_one_day
       python3 scripts/benchmark-throughput.py
       ```
       Record the mean throughput. Must be >= 30 tok/s (LAKE-04).
       If the benchmark scripts from Plan 3.1 have issues, fix them — they must produce valid measurements.

    5. If throughput is below 30 tok/s, investigate and attempt optimizations:
       - Check if other models are loaded simultaneously (`ollama ps`) — unload extras
       - Check available system memory: `vm_stat`
       - Close other heavy applications (browsers, IDEs) and re-run
       - Try setting `OLLAMA_NUM_PARALLEL=1` explicitly: `OLLAMA_NUM_PARALLEL=1 ollama serve` (restart daemon)
       - Document findings regardless of outcome

    6. If TTFT exceeds 5s, investigate:
       - First run always includes model loading — re-run benchmark (model should be cached in VRAM)
       - Check disk I/O: `sudo fs_usage -f filesys ollama` during cold start
       - Document the cold-start vs warm-start TTFT difference

    7. Save benchmark results to a file for reference:
       ```bash
       python3 scripts/benchmark-latency.py > .planning/phases/03-local-ai-serving/benchmarks-latency.txt 2>&1
       python3 scripts/benchmark-throughput.py > .planning/phases/03-local-ai-serving/benchmarks-throughput.txt 2>&1
       ```
  </action>
  <verify>
    <automated>cd /Users/bretbouchard/apps/me_code_pretty_one_day && python3 scripts/benchmark-latency.py 2>&1 | tail -5 && python3 scripts/benchmark-throughput.py 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Latency benchmark completes with mean TTFT < 5.0 seconds (or investigation documented if above)
    - Throughput benchmark completes with mean tok/s >= 30 (or investigation documented if below)
    - Benchmark results saved to .planning/phases/03-local-ai-serving/benchmarks-latency.txt and benchmarks-throughput.txt
    - Ollama serving confirmed stable on localhost:11434
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Benchmark scripts -> Ollama API | Localhost only, no external exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | D | Benchmark disk I/O spike during model load | accept | Model loads once, then stays in VRAM. No DoS vector on local machine. |
</threat_model>

<verification>
- `ollama ps` shows model loaded
- `curl http://localhost:11434/v1/models` returns model list
- Latency benchmark produces numerical TTFT output
- Throughput benchmark produces numerical tok/s output
- Benchmark files written to phase directory
- If targets not met, investigation notes exist explaining why and what was attempted
</verification>

<success_criteria>
- Mean TTFT < 5.0 seconds (warm run, model already in VRAM)
- Mean throughput >= 30 tok/s for 512-token generation
- Benchmark results persisted in phase directory
- Ollama API stable and responsive throughout benchmarks
</success_criteria>

<output>
After completion, create `.planning/phases/03-local-ai-serving/03-02-SUMMARY.md`
</output>
