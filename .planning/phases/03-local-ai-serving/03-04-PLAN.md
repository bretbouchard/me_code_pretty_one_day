---
phase: 03-local-ai-serving
plan: 04
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - scripts/verify-offline.py
autonomous: true
requirements: [LAKE-05]
user_setup: []
must_haves:
  truths:
    - "Full code generation inference works with network interface disabled"
    - "Ollama serves model without any network connectivity"
    - "No outbound network requests are made during inference"
  artifacts:
    - path: "scripts/verify-offline.py"
      provides: "Automated offline verification with network monitoring"
      min_lines: 50
  key_links:
    - from: "scripts/verify-offline.py"
      to: "Ollama API"
      via: "localhost request with network monitoring active"
      pattern: "localhost:11434"
---

<objective>
Verify that the entire serving pipeline works fully offline with zero network dependency after model download.

Purpose: Validate LAKE-05 (offline operation). This is a core project value — "never phones home." The model must generate code with all network interfaces disabled.
Output: Automated offline verification script, documented proof of offline capability.
</objective>

<execution_context>
@/Users/bretbouchard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bretbouchard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-local-ai-serving/03-02-SUMMARY.md

# Infrastructure (from Plan 3.2)
- Ollama serving qwen2.5-coder:7b-instruct-q4_K_M on localhost:11434
- Model already downloaded and cached locally
- Benchmark scripts verified working

# Offline requirement (LAKE-05)
- Full inference with zero network after model download
- This is a CORE VALUE of the project: "never phones home"
- Must verify no telemetry, no model fetching, no API calls during inference

# macOS network control
- `networksetup -setnetworkserviceenabled Wi-Fi off` disables Wi-Fi
- `networksetup -setnetworkserviceenabled Ethernet off` disables Ethernet
- `ifconfig lo0` — loopback stays up (needed for localhost)
- Alternative: Use `sudo pfctl` to block all outbound except loopback (less disruptive)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create offline verification script and test</name>
  <files>scripts/verify-offline.py</files>
  <action>
    Create an automated offline verification that proves inference works with no network:

    1. Create `scripts/verify-offline.py` that:
       - Accepts `--serve ollama|mlx` flag (default: ollama) and `--port` (default: 11434)
       - Step 1: Verify server is responding BEFORE network disconnect
       - Step 2: Record current network interfaces: `networksetup -listallnetworkservices`
       - Step 3: Check for any active non-loopback connections: `lsof -i -P | grep ESTABLISHED | grep -v 127.0.0.1 | grep -v ::1`
       - Step 4: Disable all non-loopback network interfaces:
         - For each service from `networksetup -listallnetworkservices` (skip "Loopback"):
           `networksetup -setnetworkserviceenabled {service} off`
         - Catch and report if this requires sudo (exit with clear message)
       - Step 5: Verify network is actually down: `ping -c 1 -t 2 8.8.8.8` should fail
       - Step 6: Verify loopback still works: `curl -s http://127.0.0.1:{port}/v1/models` should succeed
       - Step 7: Send 3 code generation requests to the server:
         ```python
         prompts = [
             "Write a Python function to merge two sorted lists.",
             "Implement a stack with push, pop, and peek in Python.",
             "Write a function to find the longest common substring.",
         ]
         ```
       - Step 8: Validate each response contains syntactically plausible code (check for `def ` or `class ` in output)
       - Step 9: Re-enable all network interfaces (undo Step 4)
       - Step 10: Print summary: PASS/FAIL for each step, overall PASS/FAIL
       - CRITICAL: If Step 9 fails (network doesn't come back), print emergency instructions for manual recovery

    2. Run the script:
       ```bash
       cd /Users/bretbouchard/apps/me_code_pretty_one_day
       python3 scripts/verify-offline.py
       ```
       NOTE: This script disables real network interfaces. It requires either:
       - Running with sudo, OR
       - Manual network toggle instructions printed if permission denied

    3. If sudo is not available (common in CI/automated contexts), provide a fallback mode:
       - `--no-sudo` flag that skips interface toggling and instead:
         - Uses `nettop` or `lsof` to monitor for any outbound connections during inference
         - Warns that this is a "soft" offline test (doesn't actually disconnect)
         - Still validates that inference works and monitors for unexpected connections

    4. Save verification results:
       ```bash
       python3 scripts/verify-offline.py > .planning/phases/03-local-ai-serving/offline-verification.txt 2>&1
       ```
  </action>
  <verify>
    <automated>ls /Users/bretbouchard/apps/me_code_pretty_one_day/scripts/verify-offline.py && python3 -c "import ast; ast.parse(open('/Users/bretbouchard/apps/me_code_pretty_one_day/scripts/verify-offline.py').read())"</automated>
  </verify>
  <done>
    - verify-offline.py exists and parses without syntax errors
    - Script has both sudo (hard disconnect) and --no-sudo (monitor-only) modes
    - Script re-enables network interfaces after testing (recovery step)
    - Code generation succeeds during network disconnect (or monitor-only mode shows no outbound connections)
    - Verification results saved to phase directory
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Network interface toggle | Script disables real network. Must not brick connectivity. |
| sudo requirement | Network interface changes need admin privileges. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | D | Network interfaces not re-enabled after test | mitigate | Script has explicit recovery step (Step 9). Prints manual recovery instructions if automated recovery fails. try/finally ensures cleanup runs. |
| T-03-07 | I | sudo privilege escalation | accept | Script only toggles network interfaces — no data access, no persistence changes. Minimal privilege surface. |
</threat_model>

<verification>
- verify-offline.py parses without syntax errors
- Script has recovery logic for network re-enablement
- Both sudo and --no-sudo modes documented
- Verification output saved to phase directory
</verification>

<success_criteria>
- Offline code generation produces valid Python output with network disabled
- No outbound network connections observed during inference
- Network interfaces restored to previous state after test
- Verification results documented in phase directory
</success_criteria>

<output>
After completion, create `.planning/phases/03-local-ai-serving/03-04-SUMMARY.md`
</output>
