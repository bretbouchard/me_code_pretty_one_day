---
phase: 01-rick-currency-updates
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/agents/swiftui-liquid-glass.md
autonomous: true
requirements:
  - RICK-03

must_haves:
  truths:
    - "swiftui-liquid-glass contains WWDC26 content for spatial layout, WebKit, SwiftUI+RealityKit, AttributedString, and Liquid Glass intensity switch"
    - "swiftui-liquid-glass cross-references visionos-rick for deep spatial computing coverage"
    - "swiftui-liquid-glass review checklist includes WWDC26 items"
    - "swiftui-liquid-glass YAML frontmatter is unchanged (name, description, tools, model)"
  artifacts:
    - path: "~/.claude/agents/swiftui-liquid-glass.md"
      provides: "Updated SwiftUI Liquid Glass specialist with WWDC26 additions"
      contains: "WWDC26"
  key_links:
    - from: "~/.claude/agents/swiftui-liquid-glass.md"
      to: "~/.claude/agents/visionos-rick.md"
      via: "cross-reference note"
      pattern: "visionos-rick"
---

<objective>
Update swiftui-liquid-glass with WWDC26 platform changes.

Purpose: The SwiftUI Liquid Glass specialist must reflect WWDC26 updates including Liquid Glass intensity switch, enhanced toolbar controls, spatial layout refinements for visionOS 27, AttributedString in TextEditor, and SwiftUI concurrency changes for Swift 6.4. Cross-reference visionos-rick for deep spatial computing coverage.

Output: Updated `~/.claude/agents/swiftui-liquid-glass.md` with WWDC26 content.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-rick-currency-updates/01-CONTEXT.md
@.planning/phases/01-rick-currency-updates/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add WWDC26 content to swiftui-liquid-glass body and update checklist</name>
  <files>~/.claude/agents/swiftui-liquid-glass.md</files>
  <read_first>
    - ~/.claude/agents/swiftui-liquid-glass.md
    - ~/.claude/agents/visionos-rick.md (first 50 lines for cross-reference context)
  </read_first>
  <action>
Read the full swiftui-liquid-glass.md file. The file already has extensive WWDC25 content. Add WWDC26 updates using the following concrete additions. Do NOT rewrite existing content -- augment only.

**1. Section "SwiftUI Spatial Layout" -- Add WWDC26 refinements after existing content:**
After the existing spatial layout section content (after the "Key points" list), add:
```markdown
**WWDC26 Update -- Spatial Layout Refinements for visionOS 27:**

visionOS 27 refines spatial layout APIs with improved depth management and gaze-driven positioning. Glass effects in spatial contexts now support the system-wide intensity switch.

```swift
// ✅ WWDC26: Refined spatial glass with intensity awareness
if #available(visionOS 27, *) {
    SpatialGlassContainer(depth: .medium) {
        HStack(spacing: 40) {
            FeatureCard()
                .glassEffect(.regular, in: .rect(cornerRadius: 16))
                .offset3D(x: 0, y: 0, z: -20)
        }
    }
    // System intensity switch automatically applies
    // Verify readability at all intensity levels
}
```

> **Cross-Reference:** For comprehensive spatial computing patterns (spatial gestures, gaze-driven interaction, immersion levels, shared space rendering, persona handling), see **VisionOS Rick** (`~/.claude/agents/visionos-rick.md`). This Rick covers the glass API surface -- the specialist covers the full spatial computing surface.
```

**2. Section "WebKit for SwiftUI" -- Add released SDK API details after existing content:**
The existing content says "details pending full SDK release". Now that iOS 26 SDK is released, enhance with actual API patterns. After the existing WebView code example, add:
```markdown
**Released SDK Enhancement (WWDC25, confirmed in iOS 26 SDK):**

```swift
if #available(iOS 26, *) {
    // ✅ Native SwiftUI WebView with full API surface
    WebView(url: URL(string: "https://example.com")!)
        .glassEffect(.regular, in: .rect(cornerRadius: 12))

    // Navigation callbacks via SwiftUI-native patterns
    // No more UIViewRepresentable coordinator pattern needed
    // Loading state, URL observation, JS bridge all available natively
} else {
    // Fallback: UIViewRepresentable wrapper still needed for iOS < 26
    WebViewRepresentable(url: URL(string: "https://example.com")!)
}
```

The migration path is now confirmed: Remove all UIViewRepresentable/NSViewRepresentable WKWebView wrappers when targeting iOS 26+. Use native `WebView` in SwiftUI.
```

**3. Section "AttributedString in SwiftUI" -- Add TextEditor rich text content:**
After the existing AttributedString content, add:
```markdown
**WWDC26 Update -- AttributedString in TextEditor:**

iOS 27 adds native rich text editing via AttributedString in TextEditor. No more third-party rich text editors.

```swift
if #available(iOS 27, *) {
    // ✅ Native rich text editing with AttributedString
    @State private var richText: AttributedString = AttributedString("Edit me!")

    var body: some View {
        TextEditor(text: $richText)
            .glassEffect(.regular, in: .rect(cornerRadius: 12))
            // TextEditor now natively supports AttributedString
            // Bold, italic, underline, colors all preserved
    }
}
```
```

**4. Add new section "### Liquid Glass Intensity Switch (WWDC26)" after the visionOS Glass section:**
Add as a new subsection within the "WWDC25/26 Updates" section:
```markdown
### Liquid Glass Intensity Switch (WWDC26)

iOS 27 introduces a user-controllable system-wide Liquid Glass intensity setting. Users can adjust glass density from nearly transparent to fully opaque.

**Impact on SwiftUI views:**
- All `.glassEffect()` calls automatically respect the system intensity setting
- No code changes required for adoption
- However, you MUST verify your glass layouts remain readable at all intensity levels

```swift
// ✅ VERIFY: Test glass at all system intensity levels
// No API change needed -- system handles intensity automatically
// But verify readability and visual hierarchy at:
// - Minimum intensity (near-transparent glass)
// - Default intensity
// - Maximum intensity (opaque glass)

// For cases where consistent appearance is required:
// Consider using non-glass backgrounds (solid colors, materials)
// for critical UI elements that must not vary
```

**Testing checklist:**
- [ ] Test all glass surfaces at minimum system intensity
- [ ] Verify text remains readable on glass at all intensity levels
- [ ] Check glass button tap targets remain visible
- [ ] Verify glass containers don't lose visual grouping at low intensity
- [ ] Test on visionOS where glass intensity may interact with spatial depth
```

**5. Section "SwiftUI Concurrency Patterns" -- Add Swift 6.4 default @MainActor note:**
After the existing concurrency content, add:
```markdown
**WWDC26 Update -- Swift 6.4 Default @MainActor Isolation:**

Swift 6.4 introduces enhanced default actor isolation behavior. SwiftUI views benefit from improved automatic @MainActor inference, reducing the need for explicit annotations.

```swift
// ✅ Swift 6.4: Enhanced automatic @MainActor inference
// SwiftUI views get improved automatic isolation
// Fewer explicit @MainActor annotations needed
// Better diagnostics for isolation violations

struct GlassContentView: View {
    @State private var content: Content?

    var body: some View {
        GlassEffectContainer {
            if let content {
                ContentCard(content)
            }
        }
        .task {
            // @MainActor is automatic in SwiftUI view context
            content = await loadContent()
        }
    }
}
```
```

**6. Update the Review Checklist -- Add WWDC26 section:**
After the existing "### WWDC25 Additions" section, add:
```markdown
### WWDC26 Additions
- [ ] **Liquid Glass intensity tested** -- Verify at all system intensity levels
- [ ] **AttributedString in TextEditor** -- Use native rich text editing on iOS 27+
- [ ] **Spatial layout refinements** -- Use visionOS 27 spatial APIs where applicable
- [ ] **Swift 6.4 concurrency** -- Leverage enhanced automatic @MainActor inference
- [ ] **WebKit confirmed migration** -- No UIViewRepresentable wrappers for iOS 26+ targets
```

**7. Add cross-reference to visionos-rick in spatial layout section:**
Already added in step 1 above (the spatial layout refinement section includes the cross-reference note).

**CRITICAL CONSTRAINTS:**
- Do NOT modify the YAML frontmatter (lines 1-6) -- name, description, tools, model must stay unchanged
- Do NOT rewrite existing WWDC25 content -- only add new WWDC26 content and cross-references
- Do NOT duplicate content from visionos-rick -- use cross-reference note
- Use the Edit tool with precise line ranges, not Write to rewrite the entire file
  </action>
  <verify>
    grep -c "WWDC26" ~/.claude/agents/swiftui-liquid-glass.md  # Must be >= 3
    grep -c "intensity" ~/.claude/agents/swiftui-liquid-glass.md  # Must be >= 3
    grep -c "visionos-rick" ~/.claude/agents/swiftui-liquid-glass.md  # Must be >= 1
    grep -c "AttributedString" ~/.claude/agents/swiftui-liquid-glass.md  # Must be >= 5
    grep -c "TextEditor" ~/.claude/agents/swiftui-liquid-glass.md  # Must be >= 2
    grep -c "Swift 6.4" ~/.claude/agents/swiftui-liquid-glass.md  # Must be >= 1
    head -6 ~/.claude/agents/swiftui-liquid-glass.md | grep -c "name: swiftui-liquid-glass"  # Must be 1
  </verify>
  <done>
    - YAML frontmatter unchanged (name: swiftui-liquid-glass, model: opus, same tools list)
    - Spatial Layout section has WWDC26 refinements and cross-reference to visionos-rick
    - WebKit section enhanced with released SDK API details
    - AttributedString section has TextEditor rich text editing content
    - New Liquid Glass Intensity Switch section added
    - SwiftUI Concurrency section has Swift 6.4 default @MainActor content
    - Review checklist has WWDC26 Additions section
  </done>
</task>

</tasks>

<verification>
```bash
# Verify frontmatter intact
head -6 ~/.claude/agents/swiftui-liquid-glass.md | grep "name: swiftui-liquid-glass"
head -6 ~/.claude/agents/swiftui-liquid-glass.md | grep "model: opus"

# Verify WWDC26 content
grep -c "WWDC26" ~/.claude/agents/swiftui-liquid-glass.md
grep "intensity" ~/.claude/agents/swiftui-liquid-glass.md | head -5

# Verify cross-references
grep "visionos-rick" ~/.claude/agents/swiftui-liquid-glass.md

# Verify new content areas
grep "TextEditor" ~/.claude/agents/swiftui-liquid-glass.md
grep "Swift 6.4" ~/.claude/agents/swiftui-liquid-glass.md
```
</verification>

<success_criteria>
- swiftui-liquid-glass.md contains WWDC26 content for Liquid Glass intensity, spatial layout refinements, TextEditor rich text, Swift 6.4 concurrency
- Cross-reference to visionos-rick present
- WebKit section enhanced with released SDK details
- Review checklist updated with WWDC26 Additions section
- YAML frontmatter unchanged
- No verbatim duplication of visionos-rick content
</success_criteria>

<output>
After completion, create `.planning/phases/01-rick-currency-updates/01-03-SUMMARY.md`
</output>
