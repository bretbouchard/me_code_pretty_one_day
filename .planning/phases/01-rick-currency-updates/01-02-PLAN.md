---
phase: 01-rick-currency-updates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/agents/gamer-rick.md
autonomous: true
requirements:
  - RICK-02

must_haves:
  truths:
    - "gamer-rick contains WWDC26 content for Apple Games App, Metal 4 games, Game Center, visionOS spatial accessories, and Liquid Glass"
    - "gamer-rick cross-references metal-4-rick and visionos-rick for deep domain coverage"
    - "gamer-rick review checklist includes WWDC26 items"
    - "gamer-rick YAML frontmatter is unchanged (name, description, tools, model)"
  artifacts:
    - path: "~/.claude/agents/gamer-rick.md"
      provides: "Updated Apple gaming specialist with WWDC26 additions"
      contains: "WWDC26"
  key_links:
    - from: "~/.claude/agents/gamer-rick.md"
      to: "~/.claude/agents/metal-4-rick.md"
      via: "cross-reference note"
      pattern: "metal-4-rick"
    - from: "~/.claude/agents/gamer-rick.md"
      to: "~/.claude/agents/visionos-rick.md"
      via: "cross-reference note"
      pattern: "visionos-rick"
---

<objective>
Update gamer-rick with WWDC26 platform changes for Apple gaming.

Purpose: The Apple gaming specialist Rick must reflect WWDC26 gaming updates including Logitech Muse spatial controller details, MetalFX improvements, frame interpolation, Liquid Glass intensity switch for game UIs, and any Games App updates. Cross-reference metal-4-rick and visionos-rick for deep domain coverage.

Output: Updated `~/.claude/agents/gamer-rick.md` with WWDC26 content and enhanced cross-references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-rick-currency-updates/01-CONTEXT.md
@.planning/phases/01-rick-currency-updates/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add WWDC26 content to gamer-rick body and update checklist</name>
  <files>~/.claude/agents/gamer-rick.md</files>
  <read_first>
    - ~/.claude/agents/gamer-rick.md
    - ~/.claude/agents/metal-4-rick.md (first 50 lines for cross-reference context)
    - ~/.claude/agents/visionos-rick.md (first 50 lines for cross-reference context)
  </read_first>
  <action>
Read the full gamer-rick.md file. The file already has extensive WWDC25 content across gaming domains. Add WWDC26 updates using the following concrete additions. Do NOT rewrite existing content -- augment only.

**1. Section 7 (Apple Games App) -- Add WWDC26 update at the end:**
Add before the integration checklist:
```markdown
**WWDC26 Update:**

The Apple Games App receives continued investment in iOS 27. Games targeting the Games App should ensure:
- Updated metadata for iOS 27 compatibility
- Swift 6.4 adoption in game code for improved concurrency ergonomics
- App Intents integration for Siri voice commands in games (replaces any SiriKit usage)
- Liquid Glass intensity switch compatibility for all game UI elements
```

**2. Section 7b (Game Center) -- Add WWDC26 note at end:**
```markdown
**WWDC26 Update:**

Game Center APIs continue to evolve with iOS 27. Key changes:
- Enhanced analytics pipeline with deeper retention tracking
- Updated GKMatchmaker for improved multiplayer reliability
- App Intents integration for Game Center shortcuts (replaces any SiriKit-based shortcuts)
- Progressive achievements API refinements
```

**3. Section 8 (visionOS Spatial Gaming) -- Add Logitech Muse and WWDC26 content:**
After the existing visionOS content, add:
```markdown
**WWDC26 Update -- Logitech Muse Spatial Controller:**

The Logitech Muse is a dedicated spatial controller for visionOS with precise 6DOF input. It uses the standard GameController framework but adds spatial-specific capabilities.

```swift
#if os(visionOS)
// ✅ Logitech Muse spatial controller detection
func setupSpatialController(_ controller: GCController) {
    // Muse uses extendedGamepad profile with spatial metadata
    if let extendedGamepad = controller.extendedGamepad {
        // Standard gamepad mapping
        setupSpatialGamepadMapping(extendedGamepad)

        // ✅ Spatial-specific: Check for 6DOF capabilities
        if let motion = controller.motion {
            // Muse provides precise spatial position + rotation
            // Use for spatial pointing, object manipulation
            let position = motion.gravity  // 3D position
            let rotation = motion.rotationRate  // 3D rotation
        }
    }

    // ✅ Spatial game controller badge
    // Games supporting spatial controllers should declare this
    // via their Info.plist for Games App discoverability
}
#endif
```

> **Cross-Reference:** For comprehensive spatial UI patterns, GlassEffectContainer in spatial contexts, and 3D layout with SwiftUI spatial layout APIs, see **VisionOS Rick** (`~/.claude/agents/visionos-rick.md`). This Rick covers spatial gaming input -- the specialist covers the full spatial computing surface.
```

**4. Section 9 (Metal 4 for Games) -- Add WWDC26 content and cross-reference:**
After the existing Metal 4 Migration Checklist, add:
```markdown
**WWDC26 Update -- MetalFX and Frame Interpolation:**

```cpp
// ✅ METAL 4 WWDC26: MetalFX improvements for games
// Enhanced temporal anti-aliasing with ML-driven upscaling
// Frame interpolation for smoother 60fps->120fps conversion
// Improved denoising for ray-traced scenes

// MetalFX temporal upscaling now has better quality at lower resolution
// inputs, reducing GPU workload while maintaining visual fidelity.

// Frame interpolation generates intermediate frames using ML inference
// inline with the render pipeline (Metal 4 ML+graphics convergence)
```

**MetalFX Migration Checklist Addition:**
- [ ] **MetalFX upscaling evaluation** -- Test quality vs. native rendering on target hardware
- [ ] **Frame interpolation testing** -- Verify 60fps->120fps conversion quality for ProMotion displays
- [ ] **Ray tracing denoising** -- Evaluate MetalFX denoiser for ray-traced lighting passes

> **Cross-Reference:** For deep Metal 4 GPU expertise (mesh shader implementation, MetalFX API details, compute optimization, buffer alignment, ray tracing), see **Metal 4 Rick** (`~/.claude/agents/metal-4-rick.md`). This Rick covers Metal 4 for games -- the specialist covers the full GPU API surface.
```

**5. Section 10 (Liquid Glass for Game UI) -- Add WWDC26 intensity note:**
After the existing Liquid Glass content, add:
```markdown
**WWDC26 Update -- Liquid Glass Intensity Switch:**

iOS 27 introduces a system-wide Liquid Glass intensity control. For games, this means:
- Game UI elements using `.glassEffect()` will automatically respect the user's intensity preference
- Test all glass game UI at minimum intensity (near-transparent) and maximum intensity
- Health bars, inventory cards, and HUD elements should maintain readability at all intensity levels
- Consider adding a game-specific glass intensity override if the game's UI needs consistent appearance
```

**6. Update the "Apple Platform (WWDC25/26 Updates)" checklist:**
Add these items to the existing Apple Platform checklist section:
```markdown
- [ ] **Logitech Muse spatial controller support** -- Detect and handle 6DOF spatial input on visionOS
- [ ] **MetalFX upscaling adoption** -- Evaluate for performance improvement on supported hardware
- [ ] **Frame interpolation** -- Test for ProMotion display smoothness
- [ ] **Liquid Glass intensity tested** -- Verify game UI at all system glass intensity levels
- [ ] **App Intents for Siri game commands** -- Replace any SiriKit shortcuts with App Intents
- [ ] **Swift 6.4 concurrency** -- Adopt ergonomics improvements in game loop code
```

**7. Update Motivation paragraph:**
In the Motivation section, change the quoted paragraph to include WWDC26:
```
"Platform certification is not a checklist - it's a promise to players. Gamepad usability is not an afterthought - it's how players experience your game. Apple just gave us Metal 4, Liquid Glass, spatial controllers like the Logitech Muse, and the Apple Games App -- the platform is evolving faster than ever, and the games that don't keep up will be invisible. MetalFX upscaling, frame interpolation, and spatial gaming are the new frontier. I'm here to ensure every platform certifies on first submission, every gamepad feels responsive, every pro user gets the precision they demand, and every Apple game ships ready for the Metal 4 and Liquid Glass era."
```

**8. Update Council Integration -- Collaborations:**
Add cross-reference collaboration:
```
- **VisionOS Rick**: Coordinate on spatial gaming UI patterns and spatial accessory input
```

**CRITICAL CONSTRAINTS:**
- Do NOT modify the YAML frontmatter (lines 1-14) -- name, description, tools, model must stay unchanged
- Do NOT rewrite existing WWDC25 content -- only add new WWDC26 content and cross-references
- Do NOT duplicate content from metal-4-rick or visionos-rick -- use cross-reference notes instead
- Use the Edit tool with precise line ranges, not Write to rewrite the entire file
  </action>
  <verify>
    grep -c "WWDC26" ~/.claude/agents/gamer-rick.md  # Must be >= 3
    grep -c "Logitech Muse" ~/.claude/agents/gamer-rick.md  # Must be >= 2
    grep -c "metal-4-rick" ~/.claude/agents/gamer-rick.md  # Must be >= 1
    grep -c "visionos-rick" ~/.claude/agents/gamer-rick.md  # Must be >= 1
    grep -c "MetalFX" ~/.claude/agents/gamer-rick.md  # Must be >= 3
    grep -c "intensity" ~/.claude/agents/gamer-rick.md  # Must be >= 2
    head -14 ~/.claude/agents/gamer-rick.md | grep -c "name: gamer-rick"  # Must be 1
  </verify>
  <done>
    - YAML frontmatter unchanged (name: gamer-rick, model: glm-5-turbo, same tools list)
    - Section 7 has WWDC26 Games App update
    - Section 7b has WWDC26 Game Center update
    - Section 8 has Logitech Muse spatial controller content and cross-reference to visionos-rick
    - Section 9 has MetalFX/frame interpolation WWDC26 content and cross-reference to metal-4-rick
    - Section 10 has Liquid Glass intensity switch content
    - Apple Platform checklist updated with WWDC26 items
    - Motivation paragraph updated
    - Council Integration updated with VisionOS Rick collaboration
  </done>
</task>

</tasks>

<verification>
```bash
# Verify frontmatter intact
head -14 ~/.claude/agents/gamer-rick.md | grep "name: gamer-rick"
head -14 ~/.claude/agents/gamer-rick.md | grep "model: glm-5-turbo"

# Verify WWDC26 content
grep -c "WWDC26" ~/.claude/agents/gamer-rick.md
grep "Logitech Muse" ~/.claude/agents/gamer-rick.md | head -5
grep "MetalFX" ~/.claude/agents/gamer-rick.md | head -5

# Verify cross-references
grep "metal-4-rick" ~/.claude/agents/gamer-rick.md
grep "visionos-rick" ~/.claude/agents/gamer-rick.md
```
</verification>

<success_criteria>
- gamer-rick.md contains WWDC26 content for Logitech Muse, MetalFX, frame interpolation, Game Center updates, and Liquid Glass intensity
- Cross-references to metal-4-rick and visionos-rick present
- Apple Platform checklist updated with WWDC26 items
- YAML frontmatter unchanged
- No verbatim duplication of new Rick content
</success_criteria>

<output>
After completion, create `.planning/phases/01-rick-currency-updates/01-02-SUMMARY.md`
</output>
